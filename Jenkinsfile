pipeline {
  agent any
  stages {
    stage('checkout code') {
      steps {
        git 'https://github.com/cherifissa/curriculum-app.git'
      }
    }

    stage('') {
      steps {
        sh 'ls -la'
      }
    }

  }
}